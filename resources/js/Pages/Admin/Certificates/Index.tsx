import React from 'react';
import { Head, Link } from '@inertiajs/react';
import AdminLayout from '@/Layouts/AdminLayout';
import { useTranslation } from '@/Hooks/useTranslation';
import { Award, Eye } from 'lucide-react';

interface Props {
    certificates: {
        data: Array<{
            id: number;
            uuid: string;
            certificate_code: string;
            student_name: string;
            student_email: string;
            course_title: string;
            issued_at: string;
        }>;
    };
}

export default function Index({ certificates }: Props) {
    const { t } = useTranslation();

    return (
        <AdminLayout title={t('admin.manage_certificates')}>
            <div className="space-y-6">
                <p className="text-xs text-neutral-500">
                    Audit and inspect all verified certificates generated by students completing full courses.
                </p>

                <div className="rounded-2xl border border-neutral-200 dark:border-neutral-800 bg-white dark:bg-neutral-900 overflow-hidden shadow-xs">
                    <table className="w-full text-left text-xs">
                        <thead className="bg-neutral-50 dark:bg-neutral-800/60 border-b border-neutral-200 dark:border-neutral-800 text-neutral-500 uppercase font-semibold">
                            <tr>
                                <th className="p-4">Certificate ID</th>
                                <th className="p-4">Student</th>
                                <th className="p-4">Course</th>
                                <th className="p-4">Issued At</th>
                                <th className="p-4 text-right">Verification Link</th>
                            </tr>
                        </thead>
                        <tbody className="divide-y divide-neutral-100 dark:divide-neutral-800">
                            {certificates.data.map((c) => (
                                <tr key={c.id} className="hover:bg-neutral-50 dark:hover:bg-neutral-800/40">
                                    <td className="p-4 font-mono font-bold text-primary-600">
                                        {c.certificate_code}
                                    </td>
                                    <td className="p-4">
                                        <div className="font-bold text-neutral-900 dark:text-white">
                                            {c.student_name}
                                        </div>
                                        <div className="text-neutral-400 text-[11px]">{c.student_email}</div>
                                    </td>
                                    <td className="p-4 font-semibold text-neutral-800 dark:text-neutral-200">
                                        {c.course_title}
                                    </td>
                                    <td className="p-4 text-neutral-500">{c.issued_at}</td>
                                    <td className="p-4 text-right">
                                        <Link
                                            href={`/certificates/${c.uuid}`}
                                            target="_blank"
                                            className="inline-flex items-center gap-1 px-3 py-1 bg-neutral-100 dark:bg-neutral-800 hover:bg-primary-50 text-neutral-700 dark:text-neutral-200 rounded-lg text-xs font-semibold transition-colors"
                                        >
                                            <Eye className="w-3.5 h-3.5" />
                                            <span>View</span>
                                        </Link>
                                    </td>
                                </tr>
                            ))}
                        </tbody>
                    </table>
                </div>
            </div>
        </AdminLayout>
    );
}

